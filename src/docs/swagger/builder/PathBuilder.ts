import { PathBuilderEnhanced } from './PathBuilderEnhanced';

// Export the enhanced builder as PathBuilder for compatibility
export class PathBuilder extends PathBuilderEnhanced {
  // The enhanced builder already implements all the fluent methods
  // that are being used in the path files
}